<div class="row">
  <div class="col-lg-12">
    <!-- START panel-->
    <div id="panelDemo12" class="panel panel-default">

      <div class="panel-body">
        <hr style="width: 100%">
        <table id="usuarios_datatable" class="table table-striped table-bordered table-hover display nowrap" data-source="<%= usuarios_path(format: :json) %>">
          <thead>
          <tr>
            <% colunas.each do |coluna| %>
              <th><%=coluna %></th>
            <% end %>
          </tr>
          </thead>
          <tbody>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
